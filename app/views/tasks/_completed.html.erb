  <h3> Completed Tasks </h3>
  <table class="table table-condensed table-striped">
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th>Due Date</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @completed.reverse.each do |task| %>
      <tr>
        <td><%= task.title %></td>
        <td><%= task.description %></td>
        <td><%= task.due_date %></td>
        <td><%#= link_to 'Delete', edit_task_list_task_path(list.task.id) %></td>
      </tr>
    <% end %>
  </tbody>
</table>